import 'package:flutter/material.dart';

class TopicDetail extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('topic_detail')),
      body: Container(color: Colors.greenAccent),
    );
  }
}